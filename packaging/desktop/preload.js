/* Aitzaz AI Pro — preload. Intentionally minimal: the HUD is a normal web
   page and must run with its browser security model intact. */
"use strict";
window.aitzazDesktop = { platform: process.platform, arch: process.arch };
